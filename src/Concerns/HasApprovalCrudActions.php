<?php

declare(strict_types=1);

namespace CoringaWc\FilamentActionApprovals\Concerns;

/**
 * @deprecated Operation approval support now lives in HasApprovals. Keep this trait only as a no-op compatibility layer for models that already import both traits.
 *
 * @phpstan-ignore trait.unused
 */
trait HasApprovalCrudActions {}
